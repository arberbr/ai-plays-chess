# OpenRouter API key for model interactions.
OPENROUTER_API_KEY=your-openrouter-key

# Enable mock mode when set to 1/true (bypasses network calls for local dev).
OPENROUTER_MOCK=0

# Public env vars must be prefixed with NEXT_PUBLIC_
# Example:
# NEXT_PUBLIC_APP_ENV=local
